# EventLoop

JS is synchronous and
 single threaded bydefault


## There can be async
behaviour
- with BrowseAPI - 
setTimeout, setInterval, setImmediate, nextTick
- with promises
- with event handlers

callback function that pass as argument or the parameter to another function

MODERNJAVASCRIPT is divided into 2 catogories
-common javascript(cjs)
-javascript(js)

the cmmon operations on the file or folder are 
1. file-> writeFile , readFile , appendFile
2. folder-> mkdir/Md , rmdir/rd , readdiv
3. File stat-> stat , lstat , rstat 
4. watch-> watch , unwatch
5. stream-> readstream() , writestream()

all functions are promised so it must be called with await key. 

APPEND FUNCTION:----
append functions adss line in gthe end and creates a new file if not exsistent and add the new file.

WRITE FILE:-----

CRUD project
C-create
R-retrieve
U-update
D-delete
.
assume we are making a cart related project
1. user can add any product (id,name,price,qty) into cart
2. user can see all the items of the cart
3. user can remove item from cart
4. user can also update quantity of the product
5. all the items should be stored after termination of project
